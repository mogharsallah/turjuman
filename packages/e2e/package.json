{
	"name": "@turjuman/e2e",
	"version": "0.2.0",
	"private": true,
	"description": "Turjuman deployed end-to-end tests against LocalStack (SAM stack + Lambda Function URLs + DynamoDB Streams).",
	"license": "MIT",
	"type": "module",
	"scripts": {
		"test": "vitest run",
		"typecheck": "tsc -p tsconfig.json --noEmit"
	},
	"dependencies": {
		"@aws-sdk/client-cloudformation": "^3.700.0",
		"@aws-sdk/client-dynamodb": "^3.700.0",
		"@turjuman/core": "workspace:*"
	}
}
