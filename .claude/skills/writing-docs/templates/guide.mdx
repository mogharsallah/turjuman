---
title: "[Verb-first task, e.g. Sync files with the CLI]"
description: "[Do specific task] to [achieve outcome] — ~130–160 chars, active voice."
---

{/* GUIDE / How-to. Job: get the reader through one task. Show the minimum to accomplish it; link the
    reference for the exhaustive option list. Lead with what the reader does. See doc-types.md. */}

[One or two sentences framing the task and when you'd do it. Link to the reference page for the full
catalogue of options.]

## Prerequisites

[Optional — what the reader needs first (an API key, a project, a role). Remove if none.]

## [Action-oriented heading — the task]

<Steps>
  <Step title="[First action]">
    ```bash
    [command]
    ```
  </Step>
  <Step title="[Next action]">
    [Explanation + command.]
  </Step>
</Steps>

{/* For per-surface variants (agent vs CLI vs REST), prefer <Tabs>: */}

<Tabs>
  <Tab title="MCP (agent)">
    Ask the agent: "[example natural-language prompt]"
  </Tab>
  <Tab title="CLI">
    ```bash
    [command]
    ```
  </Tab>
</Tabs>

## Verify

[Optional — how the reader confirms it worked.]

<Tip>
  [A useful shortcut or the recommended way to do this faster.]
</Tip>

## Troubleshooting

[Optional — the one or two failure modes and their fixes. An <AccordionGroup> works well here.]

## Related

[Link the matching reference page and adjacent guides, root-relative.]
