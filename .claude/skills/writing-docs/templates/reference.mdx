---
title: "[Subject] reference"
description: "Complete reference for [feature/API] — what it lists, ~130–160 chars, active voice."
---

{/* REFERENCE. Job: exhaustive and dry — every item, default, limit, edge case. Tables or fields,
    stable ordering, no narrative or opinions. Names verbatim from source (agents scrape this).
    Point to the matching guide for "how do I use this?". See doc-types.md. */}

[One sentence stating what this lists and the auth/permission needed; link the matching guide for usage.]

## [Group / category]

| Name | Purpose | Notes / limits |
|---|---|---|
| `[exact_name]` | [what it does] | [defaults, limits, edge cases] |
| `[exact_name]` | … | … |

## Parameters

{/* For structured params/props/responses, use Fields rather than a hand-built table: */}

<ParamField path="name" type="string" required>
  [What it is, constraints, default.]
</ParamField>

<ResponseField name="field" type="string">
  [What the response field contains.]
</ResponseField>

## Examples

```bash title="Basic"
[minimal example]
```

```jsonc title="Response"
{
  "field": "value"  // comment
}
```

## Related references

[Link sibling reference pages and the usage guide, root-relative.]
