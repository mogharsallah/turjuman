---
title: "turjuman CLI commands and configuration reference for Turjuman"
sidebarTitle: "CLI Commands"
description: "Full reference for the turjuman CLI — all commands, turjuman.config.json structure, auth configuration, exit codes, and --json for CI."
---

The `turjuman` CLI is purpose-built for deterministic locale file work: it pulls translations from Turjuman and writes them to your repository as locale files, and it pushes locale files back to Turjuman. Everything else — creating projects, managing keys, running QA from an agent, administering users — goes through the MCP server or the REST API.

<Note>
Deploy commands (`deploy`, `status`, `teardown`, `bootstrap`) live in the separate `@turjuman/aws-deploy` package and are not part of the core CLI documented here.
</Note>

---

## Installation

Install the CLI from the npm registry:

```bash
npm install -g @turjuman/cli
```

Once installed, the binary is available as `turjuman`:

```bash
turjuman <command>
```

---

## Configuration

Turjuman uses two configuration files with different scopes and commit policies.

### `turjuman.config.json` — project configuration

This file lives in your repository root, is created by `turjuman init`, and **should be committed to version control**. It tells the CLI which project to work with and how to map Turjuman namespaces to local file paths.

```json
{
  "projectId": "proj_xxx",
  "targets": [
    { "format": "json-nested",     "path": "locales/{locale}.json",                          "namespace": "default" },
    { "format": "android",         "path": "android/values-{locale}/strings.xml" },
    { "format": "ios-strings",     "path": "ios/{locale}.lproj/Localizable.strings" },
    { "format": "ios-stringsdict", "path": "ios/{locale}.lproj/Localizable.stringsdict" }
  ]
}
```

Each entry in `targets` maps a file format to a path pattern. The `{locale}` placeholder in `path` is substituted with each locale code when reading or writing files (e.g. `locales/fr.json`). The `namespace` field is optional and defaults to `"default"`.

### `~/.turjuman/auth.json` — machine credentials

This file stores your API URL and key for the current machine. It is created by `turjuman login` and **must not be committed to version control**.

```json
{
  "url": "https://your-turjuman-api.example.com",
  "key": "op_live_xxxxxxxxxxxxxx"
}
```

### CI environment variables

In CI pipelines, use environment variables instead of `auth.json`:

| Variable | Purpose |
|---|---|
| `TURJUMAN_API_URL` | Base URL of your Turjuman instance |
| `TURJUMAN_API_KEY` | API key for authentication |

Environment variables take precedence over `~/.turjuman/auth.json` when both are present.

---

## Commands

| Command | What it does |
|---|---|
| `login --url <url> --key <key>` | Save credentials to `~/.turjuman/auth.json` for this machine |
| `init --project <id> [--format <fmt>] [--path <pattern>] [--namespace <ns>]` | Scaffold a `turjuman.config.json` in the current directory |
| `projects` | List all projects your API key can access |
| `locales` | List the locales configured for the current project |
| `formats` | List all supported file format IDs |
| `pull [--working] [--exclude-stale]` | Download translations from Turjuman and write locale files to disk |
| `build` | Alias for `pull` — useful in build scripts |
| `push [--prune] [--dry-run] [--check]` | Read locale files and upload translations to Turjuman |
| `check [--locale <code>] [--check <id...>] [--approved]` | Run advisory QA checks and print the findings report |

### `pull` options

| Flag | Effect |
|---|---|
| `--working` | Pull the current working (draft) values instead of the last-approved values |
| `--exclude-stale` | Skip translations whose source value has changed since they were last written |

### `push` options

| Flag | Effect |
|---|---|
| `--prune` | Delete keys in Turjuman that are absent from the pushed files |
| `--dry-run` | Compute the diff and print what would change without writing anything |
| `--check` | After pushing, run QA checks and exit with code `1` if there are error-severity findings |

### `check` options

| Flag | Effect |
|---|---|
| `--locale <code>` | Restrict checks to a single locale (may be repeated) |
| `--check <id>` | Run only the specified check(s) (may be repeated) |
| `--approved` | Only check translations that have been approved (skips draft values) |

---

## `--json` flag

Every command accepts `--json` to produce machine-readable output. When `--json` is set:

- Standard output contains a single JSON object or array.
- Progress messages and spinners are suppressed.
- Diagnostic messages (warnings, errors) are written to **stderr** only.

This flag is designed for CI pipelines and scripting where you need to parse command output programmatically.

```bash
turjuman pull --json | jq '.locales'
turjuman check --json > findings.json
```

---

## Exit codes

| Code | Meaning |
|---|---|
| `0` | Success — command completed without issues |
| `1` | QA findings with error severity were found |
| `2` | Usage error — bad flags, missing `turjuman.config.json`, or not logged in |
| `3` | API or network error — the server returned an error response or was unreachable |

<Note>
API error responses include a `requestId` field. You can provide this ID when reporting issues — it correlates directly with the corresponding entry in your Turjuman server logs.
</Note>

---

## Supported formats

Run `turjuman formats` to get the live list from your Turjuman instance. The currently supported format IDs are:

| Format ID | File type |
|---|---|
| `json-nested` | `.json` |
| `json-flat` | `.json` |
| `yaml` | `.yaml` / `.yml` |
| `arb` | `.arb` |
| `properties` | `.properties` |
| `csv` | `.csv` |
| `android` | `strings.xml` |
| `ios-strings` | `.strings` |
| `ios-stringsdict` | `.stringsdict` |

See the [File Formats reference](/reference/file-formats) for a full description of each format including plural handling.
